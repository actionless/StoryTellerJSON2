<!-- start snippet "ImageParallax" -->
<section class="part parallax" name="{{navigation.id}}">
  <div class="bg-image"
    data-src="{{ image.srcmain }}"
    data-srcMedium="{{ image.srcmedium }}"
    data-srcPhone="{{ image.srcphone }}"

  >
  </div>
  {{! @TODO: check if subcontent should work with image parallax  }}
  {{> snippets/subcontent }}
</section>
<!-- end snippet "ImageParallax" -->
